<!--pages/tasklist/tasklist.wxml-->
<!-- index:{{index}},item:{{itemindex}} -->
<view>
  <van-cell wx:for="{{taskList}}" wx:key="index" title="{{item}}" is-link url="/pages/taskdetails/askdetails?itemindex={{itemindex}}&index={{index}}" />

</view>
